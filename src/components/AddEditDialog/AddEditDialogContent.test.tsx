import React from 'react';
import { render } from '@testing-library/react';

describe('AddEditDialogContent', () => {
  it('renders without crashing', () => {
    render(<div>Mock AddEditDialogContent</div>);
  });
});
