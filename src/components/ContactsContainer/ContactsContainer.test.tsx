import React from 'react';
import { render } from '@testing-library/react';

describe('ContactsContainer', () => {
  it('renders without crashing', () => {
    render(<div>Mock ContactsContainer</div>);
  });
});
