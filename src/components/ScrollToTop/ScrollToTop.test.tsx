import React from 'react';
import { render } from '@testing-library/react';

describe('ScrollToTop', () => {
  it('renders without crashing', () => {
    render(<div>Mock ScrollToTop</div>);
  });
});
