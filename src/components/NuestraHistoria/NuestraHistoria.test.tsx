import React from 'react';
import { render, screen } from '@testing-library/react';

describe('NuestraHistoria', () => {
  it('renders without crashing', () => {
    render(<div>Mock NuestraHistoria</div>);
  });
});
