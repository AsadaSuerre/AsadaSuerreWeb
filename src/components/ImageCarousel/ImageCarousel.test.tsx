import React from 'react';
import { render, screen } from '@testing-library/react';

describe('ImageCarousel', () => {
  it('renders without crashing', () => {
    render(<div>Mock ImageCarousel</div>);
  });
});
